{
  "name": "timezone-gclid-redirect",
  "version": "1.0.0",
  "description": "Redirect users only if timezone is Japan and gclid is present",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
